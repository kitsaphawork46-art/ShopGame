import Link from "next/link";
import { Chrome, LockKeyhole, Mail, UserRound } from "lucide-react";
import { Logo } from "@/components/ui/Logo";

export function AuthCard({ mode }: { mode: "login" | "register" | "forgot" }) {
  const isLogin = mode === "login";
  const isRegister = mode === "register";
  return <main className="relative flex min-h-screen items-center justify-center overflow-hidden bg-[#030711] px-5 py-16"><div className="grid-texture absolute inset-0 opacity-50" /><div className="absolute left-1/2 top-1/2 h-[480px] w-[480px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-blue-600/10 blur-[100px]" /><div className="relative w-full max-w-md rounded-3xl border border-slate-800 bg-[#07111f]/90 p-7 shadow-2xl backdrop-blur sm:p-9"><div className="flex justify-center"><Logo /></div><div className="mt-8 text-center"><p className="section-kicker">{isLogin ? "Welcome back" : isRegister ? "Join the loot" : "Account recovery"}</p><h1 className="mt-2 text-2xl font-black">{isLogin ? "เข้าสู่ระบบ" : isRegister ? "สร้างบัญชีใหม่" : "ลืมรหัสผ่าน"}</h1><p className="mt-2 text-sm text-slate-500">{isLogin ? "กลับมาล่า Loot ชิ้นต่อไปของคุณ" : isRegister ? "สมัครฟรี รับโค้ด BERRY20 ทันที" : "เราจะส่งลิงก์ตั้งรหัสผ่านใหม่ทางอีเมล"}</p></div><form className="mt-7 space-y-4">{isRegister && <label className="block text-xs font-bold text-slate-400">ชื่อผู้ใช้<div className="relative mt-2"><UserRound className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-600" size={17} /><input required className="field pl-11" placeholder="lootplayer" /></div></label>}<label className="block text-xs font-bold text-slate-400">อีเมล<div className="relative mt-2"><Mail className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-600" size={17} /><input required type="email" className="field pl-11" placeholder="you@example.com" /></div></label>{mode !== "forgot" && <label className="block text-xs font-bold text-slate-400">รหัสผ่าน<div className="relative mt-2"><LockKeyhole className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-600" size={17} /><input required type="password" className="field pl-11" placeholder="อย่างน้อย 8 ตัวอักษร" /></div></label>}{isRegister && <label className="block text-xs font-bold text-slate-400">ยืนยันรหัสผ่าน<input required type="password" className="field mt-2" placeholder="กรอกรหัสผ่านอีกครั้ง" /></label>}{isLogin && <div className="flex items-center justify-between text-xs"><label className="flex items-center gap-2 text-slate-400"><input type="checkbox" className="accent-blue-500" />จดจำฉัน</label><Link href="/forgot-password" className="font-bold text-cyan">ลืมรหัสผ่าน?</Link></div>}<Link href={mode === "forgot" ? "/login" : "/dashboard"} className="button-primary w-full">{isLogin ? "เข้าสู่ระบบ" : isRegister ? "สมัครสมาชิก" : "ส่งลิงก์รีเซ็ต"}</Link></form>{mode !== "forgot" && <><div className="my-6 flex items-center gap-3 text-[10px] text-slate-600"><span className="h-px flex-1 bg-slate-800" />หรือ<span className="h-px flex-1 bg-slate-800" /></div><button className="button-secondary w-full"><Chrome size={17} /> ดำเนินการต่อด้วย Google</button></>}<p className="mt-7 text-center text-xs text-slate-500">{isLogin ? "ยังไม่มีบัญชี?" : isRegister ? "มีบัญชีอยู่แล้ว?" : "จำรหัสผ่านได้แล้ว?"} <Link href={isLogin ? "/register" : "/login"} className="font-bold text-cyan">{isLogin ? "สมัครสมาชิก" : "เข้าสู่ระบบ"}</Link></p><Link href="/" className="mt-5 block text-center text-xs text-slate-600 hover:text-white">← กลับหน้าแรก</Link></div></main>;
}
