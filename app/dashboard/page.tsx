import Link from "next/link";
import { Clock3, CreditCard, Heart, PackageCheck, ShoppingBag, WalletCards } from "lucide-react";
import { DashboardShell } from "@/components/dashboard/DashboardShell";
import { products } from "@/data/products";

const orders = [{ id: "#LB2609-1842", product: "Call of Duty Points 1,100 CP", date: "4 ก.ย. 2026", price: 329, status: "กำลังจัดส่ง" }, { id: "#LB2508-1591", product: "COD Battle Pass Premium", date: "25 ส.ค. 2026", price: 259, status: "สำเร็จ" }, { id: "#LB1208-1438", product: "Valorant Points 1,000 VP", date: "12 ส.ค. 2026", price: 289, status: "สำเร็จ" }];

export default function DashboardPage() { const stats = [{ l: "ออเดอร์ทั้งหมด", n: "18", i: ShoppingBag, c: "text-blue-400" }, { l: "กำลังดำเนินการ", n: "1", i: Clock3, c: "text-amber-400" }, { l: "ยอดใช้จ่าย", n: "฿6,840", i: CreditCard, c: "text-violet-400" }, { l: "LootBerry Wallet", n: "฿450", i: WalletCards, c: "text-emerald-400" }]; return <DashboardShell><div className="flex items-end justify-between"><div><p className="section-kicker">Overview</p><h1 className="mt-2 text-3xl font-black">ภาพรวมบัญชี</h1></div><Link href="/shop" className="button-primary hidden sm:flex">เลือกซื้อสินค้า</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ l, n, i: Icon, c }) => <div key={l} className="rounded-2xl border border-slate-800 bg-[#091120] p-5"><div className="flex items-center justify-between"><span className="text-xs text-slate-500">{l}</span><Icon size={19} className={c} /></div><p className="mt-3 text-2xl font-black">{n}</p></div>)}</div><section id="orders" className="mt-8 overflow-hidden rounded-2xl border border-slate-800 bg-[#091120]"><div className="flex items-center justify-between border-b border-slate-800 p-5"><h2 className="font-bold">คำสั่งซื้อล่าสุด</h2><button className="text-xs font-bold text-cyan">ดูทั้งหมด</button></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-950/60 text-xs text-slate-500"><tr><th className="px-5 py-3">Order ID</th><th className="px-5 py-3">สินค้า</th><th className="px-5 py-3">วันที่</th><th className="px-5 py-3">ราคา</th><th className="px-5 py-3">สถานะ</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id} className="border-t border-slate-800/70"><td className="px-5 py-4 font-bold text-cyan">{order.id}</td><td className="px-5 py-4 font-semibold">{order.product}</td><td className="px-5 py-4 text-slate-500">{order.date}</td><td className="px-5 py-4">฿{order.price}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold ${order.status === "สำเร็จ" ? "bg-emerald-500/10 text-emerald-400" : "bg-amber-500/10 text-amber-400"}`}>{order.status}</span></td></tr>)}</tbody></table></div></section><section id="favorites" className="mt-8"><div className="mb-5 flex items-center gap-2"><Heart size={19} className="text-rose-400" /><h2 className="font-bold">สินค้าโปรด</h2></div><div className="grid gap-4 sm:grid-cols-3">{products.slice(0, 3).map((product) => <Link href={`/product/${product.slug}`} key={product.slug} className="flex items-center gap-4 rounded-2xl border border-slate-800 bg-[#091120] p-4 hover:border-blue-400/40"><span className={`flex h-12 w-12 items-center justify-center rounded-xl bg-gradient-to-br ${product.accent} font-black text-cyan`}>{product.mark}</span><div><p className="text-sm font-bold">{product.name}</p><p className="mt-1 text-xs text-cyan">฿{product.price.toLocaleString()}</p></div></Link>)}</div></section></DashboardShell>; }
